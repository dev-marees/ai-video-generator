# Base URL for the backend API.
# During local dev you can leave this as "/api" to use the Vite proxy
# (configured in vite.config.ts to forward to http://localhost:7273).
# Or point it directly at your backend, e.g. http://localhost:7273
VITE_API_BASE_URL=/api
